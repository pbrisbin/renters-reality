#!/bin/bash -ex

(
  cd ./vagrant
  vagrant ssh -c 'cd /app &&
                  cabal clean &&
                  cabal configure -fproduction &&
                  cabal build'
)

strip dist/build/renters/renters
cp dist/build/renters/renters .

git commit ./renters -m 'new binary'

git push heroku master
