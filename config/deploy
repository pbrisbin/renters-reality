#!/bin/bash -e

ip="${1:-rentersreality.com}"

deptag # tag deployments

touch ./**/* # CPP declarations
cabal clean
cabal install

ssh "$ip" '
  cd renters             &&
  bin/selfupdate.sh      &&
  bin/service.sh restart &&
  sleep 3
'
